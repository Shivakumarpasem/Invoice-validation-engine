"""Cleaning / standardization layer.

Normalizes vendor names, dates, and amounts into a consistent form so downstream
checks (especially duplicate detection) compare apples to apples. Built in Step 4."""
