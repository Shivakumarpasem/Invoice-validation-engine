"""Synthetic invoice data generator.

Produces realistic AP/AR invoices with *controllable messiness* (exact
duplicates, near-duplicates, integrity errors) so the validation layer has
something real to catch. Built in Step 3."""
